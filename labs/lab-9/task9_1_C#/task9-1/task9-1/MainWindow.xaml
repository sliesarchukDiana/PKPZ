<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="L1 → L2 Processor" Height="380" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button x:Name="BtnSaveL1" Content="Save L1" Width="120" Margin="0 0 10 0"
                    Click="BtnSaveL1_Click"/>
            <Button x:Name="BtnLoad" Content="Load L1 File" Width="120" Margin="0 0 10 0"
                    Click="BtnLoad_Click"/>
            <Button x:Name="BtnProcess" Content="Process" Width="120" Margin="0 0 10 0"
                    Click="BtnProcess_Click"/>
            <Button x:Name="BtnSave" Content="Save L2" Width="120"
                    Click="BtnSave_Click"/>
        </StackPanel>

        <TextBox x:Name="TxtL1" Grid.Row="1" Height="60"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" />

        <ListBox x:Name="ListL2" Grid.Row="2" Margin="0 10 0 10" />

        <TextBlock Grid.Row="3" Text="L2 saved with 5 letters per line."
                   Foreground="Gray"/>
    </Grid>
</Window>